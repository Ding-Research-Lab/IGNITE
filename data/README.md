# Local data tree

This directory is the complete upload payload for the Hugging Face Dataset. Each aligned sample contains radiometric TIFF, thermal rendering, aligned RGB, overlay and `mask_80c.png`.
